# --- Telegram ---
aiogram>=3.13,<4.0          # асинхронный фреймворк для Telegram Bot API

# --- ИИ (Google Gemini) ---
google-genai>=1.0.0         # официальный SDK Google GenAI (фото + голос)

# --- ИИ (Qwen / Alibaba DashScope, OpenAI-совместимый) ---
openai>=1.40.0              # используем для вызова Qwen по OpenAI-совместимому API
av>=12.0.0                  # конвертация голосовых ogg/opus -> wav (ffmpeg в комплекте)

# --- База данных ---
aiosqlite>=0.20.0           # асинхронный SQLite

# --- Планировщик напоминаний ---
APScheduler>=3.10,<4.0

# --- Разбор времени из текста ("завтра в 18:00" и т.п.) ---
dateparser>=1.2.0

# --- Конфиг и утилиты ---
python-dotenv>=1.0.0        # чтение .env
tzdata>=2024.1              # часовые пояса (нужно на Windows)
